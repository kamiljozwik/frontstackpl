import React from 'react';
import PropTypes from 'prop-types';
import './FSLogo.scss';

const FSLogo = () => (
  <div className="frontstack--logo">
    <div className="text front">FRONT</div>
    <div className="text stack">STACK</div>
    <div className="text pl">PL</div>
  </div>
);

export default FSLogo;
